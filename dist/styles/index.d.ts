import { ViewStyle, TextStyle } from 'react-native';
interface CustomCalendarStyles {
    row: ViewStyle;
    cell: ViewStyle;
    header: ViewStyle;
    cellText: TextStyle;
    navButton: TextStyle;
    container: ViewStyle;
    headerText: TextStyle;
}
export declare const styles: CustomCalendarStyles;
export {};
